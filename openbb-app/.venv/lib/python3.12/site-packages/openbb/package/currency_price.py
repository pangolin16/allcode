"""Autogenerated OpenBB /currency/price Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField

class ROUTER_currency_price(Container):
    """/currency/price
    historical
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def historical(
        self,
        symbol: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    'Symbol to get data for. Can use CURR1-CURR2 or'
                    'CURR1CURR2 format. Multiple comma separated items'
                    'allowed for provider(s): fmp, tiingo, yfinance.'
                )
            )
        ],
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fmp', 'tiingo', 'yfinance']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fmp, tiingo, yfinance.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """    Currency Historical Price. Currency historical data.

Currency historical prices refer to the past exchange rates of one currency against
another over a specific period.
This data provides insight into the fluctuations and trends in the foreign exchange market,
helping analysts, traders, and economists understand currency performance,
evaluate economic health, and make predictions about future movements.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp, tiingo, yfinance.
symbol : str | list[str]
    Symbol to get data for. Can use CURR1-CURR2 or CURR1CURR2 format. Multiple comma separated items allowed for provider(s): fmp, tiingo, yfinance.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
interval : str
    Time interval of the data to return. (provider: fmp,tiingo, yfinance)
    Choices for fmp: '1m', '5m', '1h', '1d'
    Choices for tiingo: '1m', '5m', '15m', '30m', '90m', '1h', '2h', '4h', '1d', '5d', '21d'
    Choices for yfinance: '1m', '2m', '5m', '15m', '30m', '60m', '90m', '1h', '1d', '5d', '1W', '1M', '1Q'

Returns
-------
OBBject
    results : list[CurrencyHistorical]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

CurrencyHistorical
------------------
date : date | datetime
    The date of the data.
open : float | None
    The open price.
high : float | None
    The high price.
low : float | None
    The low price.
close : float
    The close price.
volume : float | None
    The trading volume.
vwap : float | None
    Volume Weighted Average Price over the period.
change : float | None
    Change in the price from the previous close. (provider: fmp)
change_percent : float | None
    Percent change in the price from the previous close. (provider: fmp)

Examples
--------
>>> from openbb import obb
>>> obb.currency.price.historical(symbol='EURUSD')
>>> # Filter historical data with specific start and end date.
>>> obb.currency.price.historical(symbol='EURUSD', start_date='2023-01-01', end_date='2023-12-31')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/currency/price/historical",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "currency.price.historical",
                        ('fmp', 'tiingo', 'yfinance'),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'symbol': {'fmp': {'multiple_items_allowed': True, 'choices': None}, 'tiingo': {'multiple_items_allowed': True, 'choices': None}, 'yfinance': {'multiple_items_allowed': True, 'choices': None}}, 'interval': {'fmp': {'choices': ['1m', '5m', '1h', '1d']}, 'tiingo': {'multiple_items_allowed': False, 'choices': ['1m', '5m', '15m', '30m', '90m', '1h', '2h', '4h', '1d', '5d', '21d']}, 'yfinance': {'multiple_items_allowed': False, 'choices': ['1m', '2m', '5m', '15m', '30m', '60m', '90m', '1h', '1d', '5d', '1W', '1M', '1Q']}}},
            )
        )
