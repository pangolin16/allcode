"""Autogenerated OpenBB /regulators/sec Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
from typing import Annotated, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_regulators_sec(Container):
    """/regulators/sec
    cik_map
    filing_headers
    htm_file
    institutions_search
    rss_litigation
    schema_files
    sic_search
    symbol_map
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def cik_map(
        self,
        symbol: Annotated[
            str,
            OpenBBField(
                description='Symbol to get data for.'
            )
        ],
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Map a ticker symbol to a CIK number.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
symbol : str
    Symbol to get data for.
use_cache : bool | None
    Whether or not to use cache for the request, default is True. (provider: sec)

Returns
-------
OBBject
    results : CikMap
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

CikMap
------
cik : str | int | None
    Central Index Key (CIK) for the requested entity.

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.cik_map(symbol='MSFT')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/cik_map",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.cik_map",
                        ('sec',),
                    )
                },
                standard_params={
                    "symbol": symbol,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def filing_headers(
        self,
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Download the index headers, and cover page if available, for any SEC filing.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
url : str
    URL for the SEC filing. The specific URL is not directly used or downloaded, but is used to generate the base URL for the filing. e.g. https://www.sec.gov/Archives/edgar/data/317540/000031754024000045/coke-20240731.htm and https://www.sec.gov/Archives/edgar/data/317540/000031754024000045/ are both valid URLs for the same filing. (provider: sec)
use_cache : bool
    Use cache for the index headers and cover page. Default is True. (provider: sec)

Returns
-------
OBBject
    results : SecFiling
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SecFiling
---------
base_url : str | None
    Base URL of the filing. (provider: sec)
name : str | None
    Name of the entity filing. (provider: sec)
cik : str | None
    Central Index Key. (provider: sec)
trading_symbols : list | None
    Trading symbols, if available. (provider: sec)
sic : str | None
    Standard Industrial Classification. (provider: sec)
sic_organization_name : str | None
    SIC Organization Name. (provider: sec)
filing_date : date | None
    Filing date. (provider: sec)
period_ending : date | None
    Date of the ending period for the filing, if available. (provider: sec)
fiscal_year_end : str | None
    Fiscal year end of the entity, if available. Format: MM-DD (provider: sec)
document_type : str | None
    Specific SEC filing type. (provider: sec)
has_cover_page : bool | None
    True if the filing has a cover page. (provider: sec)
description : str | None
    Description of attached content, mostly applicable to 8-K filings. (provider: sec)
cover_page : dict | None
    Cover page information, if available. (provider: sec)
document_urls : list | None
    list of files associated with the filing. (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.filing_headers(url='https://www.sec.gov/Archives/edgar/data/317540/000119312524076556/d645509ddef14a.htm')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/filing_headers",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.filing_headers",
                        ('sec',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
                info={'url': {'sec': {'x-widget_config': {'label': 'Filing URL'}}}},
            )
        )

    @exception_handler
    @validate
    def htm_file(
        self,
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Download a raw HTML object from the SEC website.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
url : str
    URL for the SEC filing. (provider: sec)
use_cache : bool
    Cache the file for use later. Default is True. (provider: sec)

Returns
-------
OBBject
    results : SecHtmFile
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SecHtmFile
----------
url : str | None
    URL of the downloaded file. (provider: sec)
content : str | None
    Raw content of the HTM/HTML file. (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.htm_file(url='https://www.sec.gov/Archives/edgar/data/1723690/000119312525030074/d866336dex991.htm')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/htm_file",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.htm_file",
                        ('sec',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def institutions_search(
        self,
        query: Annotated[
            str,
            OpenBBField(
                description='Search query.'
            )
        ] = '',
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Search SEC-regulated institutions by name and return a list of results with CIK numbers.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
query : str
    Search query.
use_cache : bool | None
    Whether or not to use cache. (provider: sec)

Returns
-------
OBBject
    results : list[InstitutionsSearch]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

InstitutionsSearch
------------------
name : str | None
    The name of the institution. (provider: sec)
cik : str | int | None
    Central Index Key (CIK) (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.institutions_search()
>>> obb.regulators.sec.institutions_search(query='blackstone real estate')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/institutions_search",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.institutions_search",
                        ('sec',),
                    )
                },
                standard_params={
                    "query": query,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def rss_litigation(
        self,
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get the RSS feed that provides links to litigation releases concerning civil lawsuits brought by the Commission in federal court.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.

Returns
-------
OBBject
    results : list[RssLitigation]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

RssLitigation
-------------
published : datetime | None
    The date of publication. (provider: sec)
title : str | None
    The title of the release. (provider: sec)
summary : str | None
    Short summary of the release. (provider: sec)
id : str | None
    The identifier associated with the release. (provider: sec)
link : str | None
    URL to the release. (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.rss_litigation()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/rss_litigation",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.rss_litigation",
                        ('sec',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def schema_files(
        self,
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Explore SEC and FASB XBRL taxonomy schemas, labels, and presentation structures.

- No parameters: list all available taxonomy families.
- taxonomy only: get all parsed structures for the most recent year.
- taxonomy + year: get all parsed structures for a specific year.
- taxonomy + component: get one component's structure using the most recent year.
- taxonomy + year + component: get one component's parsed structure.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
taxonomy : str
    Taxonomy family to explore. Omit to list all available taxonomies and their descriptions. (provider: sec)
    Choices for sec: 'us-gaap', 'srt', 'dei', 'ecd', 'cyd', 'ffd', 'ifrs', 'hmrc-dpl', 'rxp', 'spac' 'cef', 'oef', 'vip', 'fnd', 'sro', 'sbs', 'rocr', 'country', 'currency', 'exch' 'naics', 'sic', 'stpr', 'snj'
year : int | None
    Taxonomy year (e.g. 2011+ for us-gaap, varies by taxonomy). Defaults to the most recent year when omitted. (provider: sec)
component : str | None
    Presentation component to retrieve. Values are taxonomy-specific. Omit to return all components for the taxonomy. (provider: sec)
category : str
    Filter taxonomies by SEC filer category. (provider: sec)
    Choices for sec: 'operating_company', 'investment_company', 'self_regulatory_org' 'sbs_repository', 'nrsro', 'common_reference'

Returns
-------
OBBject
    results : list[SchemaFiles]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SchemaFiles
-----------
name : str | None
    Identifier: taxonomy key, year, component name, or XBRL element ID depending on the query mode. (provider: sec)
label : str | None
    Human-readable label. (provider: sec)
description : str | None
    Description or long name. (provider: sec)
category : str | None
    Taxonomy category (e.g., operating_company). (provider: sec)
style : str | None
    Taxonomy style (e.g., FASB_STANDARD, SEC_EMBEDDED). (provider: sec)
has_label_linkbase : bool | None
    Whether the taxonomy has a parseable label linkbase. (provider: sec)
url : str | None
    URL to the taxonomy resource or SEC reference page. (provider: sec)
level : int | None
    Hierarchy depth in presentation structure (0 = root). (provider: sec)
order : float | None
    Sort order within parent in presentation structure. (provider: sec)
parent_id : str | None
    Parent XBRL element ID in presentation structure. (provider: sec)
preferred_label : str | None
    Preferred label role URI for presentation rendering. (provider: sec)
xbrl_type : str | None
    XBRL data type (e.g., monetaryItemType, textBlockItemType, stringItemType, sharesItemType, perShareItemType, domainItemType). (provider: sec)
period_type : str | None
    Period type: 'instant' (point-in-time) or 'duration' (over a period). (provider: sec)
balance_type : str | None
    Balance type for monetary items: 'credit' or 'debit'. (provider: sec)
abstract : bool | None
    Whether the element is abstract (grouping/heading only, not taggable). (provider: sec)
substitution_group : str | None
    XBRL substitution group: 'item' (line item), 'dimensionItem' (axis), 'hypercubeItem' (table). (provider: sec)
nillable : bool | None
    Whether the element value can be nil. (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.schema_files()
>>> # Explore XBRL taxonomies progressively.
>>> # list all available taxonomy families
>>> obb.regulators.sec.schema_files(provider='sec')
>>> # list components for US GAAP (latest year)
>>> obb.regulators.sec.schema_files(taxonomy='us-gaap', provider='sec')
>>> # list presentation components for US GAAP 2024
>>> obb.regulators.sec.schema_files(taxonomy='us-gaap', year=2024, provider='sec')
>>> # Get the Statement of Income presentation structure
>>> obb.regulators.sec.schema_files(taxonomy='us-gaap', year=2024, component='soi', provider='sec')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/schema_files",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.schema_files",
                        ('sec',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
                info={'taxonomy': {'sec': {'choices': ['us-gaap', 'srt', 'dei', 'ecd', 'cyd', 'ffd', 'ifrs', 'hmrc-dpl', 'rxp', 'spac', 'cef', 'oef', 'vip', 'fnd', 'sro', 'sbs', 'rocr', 'country', 'currency', 'exch', 'naics', 'sic', 'stpr', 'snj']}}, 'category': {'sec': {'choices': ['operating_company', 'investment_company', 'self_regulatory_org', 'sbs_repository', 'nrsro', 'common_reference']}}},
            )
        )

    @exception_handler
    @validate
    def sic_search(
        self,
        query: Annotated[
            str,
            OpenBBField(
                description='Search query.'
            )
        ] = '',
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Search for Industry Titles, Reporting Office, and SIC Codes. An empty query string returns all results.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
query : str
    Search query.
use_cache : bool | None
    Whether or not to use cache. (provider: sec)

Returns
-------
OBBject
    results : list[SicSearch]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SicSearch
---------
sic : int | None
    Sector Industrial Code (SIC) (provider: sec)
industry : str | None
    Industry title. (provider: sec)
office : str | None
    Reporting office within the Corporate Finance Office (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.sic_search()
>>> obb.regulators.sec.sic_search(query='real estate investment trusts')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/sic_search",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.sic_search",
                        ('sec',),
                    )
                },
                standard_params={
                    "query": query,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def symbol_map(
        self,
        query: Annotated[
            str,
            OpenBBField(
                description='Search query.'
            )
        ],
        use_cache: Annotated[
            bool | None,
            OpenBBField(
                description=(
                    'Whether or not to use cache. If True, cache will'
                    'store for seven days.'
                )
            )
        ] = True,
        provider: Annotated[
            Optional[Literal['sec']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: sec.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Map a CIK number to a ticker symbol, leading 0s can be omitted or included.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: sec.
query : str
    Search query.
use_cache : bool | None
    Whether or not to use cache. If True, cache will store for seven days.

Returns
-------
OBBject
    results : SymbolMap
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SymbolMap
---------
symbol : str | None
    Symbol representing the entity requested in the data. (provider: sec)

Examples
--------
>>> from openbb import obb
>>> obb.regulators.sec.symbol_map(query='0000789019')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/regulators/sec/symbol_map",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "regulators.sec.symbol_map",
                        ('sec',),
                    )
                },
                standard_params={
                    "query": query,
                    "use_cache": use_cache,
                },
                extra_params=kwargs,
            )
        )
