"""Autogenerated OpenBB /crypto/price Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField

class ROUTER_crypto_price(Container):
    """/crypto/price
    historical
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def historical(
        self,
        symbol: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    'Symbol to get data for. Multiple comma separated'
                    'items allowed for provider(s): fmp, tiingo,'
                    'yfinance.'
                )
            )
        ],
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fmp', 'tiingo', 'yfinance']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fmp, tiingo, yfinance.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get historical price data for cryptocurrency pair(s) within a provider.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp, tiingo, yfinance.
symbol : str | list[str]
    Symbol to get data for. Multiple comma separated items allowed for provider(s): fmp, tiingo, yfinance.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
interval : str
    Time interval of the data to return. (provider: fmp,tiingo, yfinance)
    Choices for fmp: '1m', '5m', '1h', '1d'
    Choices for tiingo: '1m', '5m', '15m', '30m', '90m', '1h', '2h', '4h', '1d', '7d', '30d'
    Choices for yfinance: '1m', '2m', '5m', '15m', '30m', '60m', '90m', '1h', '1d', '5d', '1W', '1M', '1Q'
exchanges : list[str] | str | None
    To limit the query to a subset of exchanges e.g. ['POLONIEX', 'GDAX'] Multiple comma separated items allowed. (provider: tiingo)

Returns
-------
OBBject
    results : list[CryptoHistorical]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

CryptoHistorical
----------------
date : date | datetime
    The date of the data.
open : float | None
    The open price.
high : float | None
    The high price.
low : float | None
    The low price.
close : float
    The close price.
volume : float | None
    The trading volume.
vwap : float | None
    Volume Weighted Average Price over the period.
change : float | None
    Change in the price from the previous close. (provider: fmp)
change_percent : float | None
    Change in the price from the previous close, as a normalized percent. (provider: fmp)
transactions : int | None
    Number of transactions for the symbol in the time period. (provider: tiingo)
volume_notional : float | None
    The last size done for the asset on the specific date in the quote currency. The volume of the asset on the specific date in the quote currency. (provider: tiingo)

Examples
--------
>>> from openbb import obb
>>> obb.crypto.price.historical(symbol='BTCUSD')
>>> obb.crypto.price.historical(symbol='BTCUSD', start_date='2024-01-01', end_date='2024-01-31')
>>> # Get monthly historical prices from Yahoo Finance for Ethereum.
>>> obb.crypto.price.historical(symbol='ETH-USD', interval='1m', start_date='2024-01-01', end_date='2024-12-31')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/crypto/price/historical",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "crypto.price.historical",
                        ('fmp', 'tiingo', 'yfinance'),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'symbol': {'fmp': {'multiple_items_allowed': True, 'choices': None}, 'tiingo': {'multiple_items_allowed': True, 'choices': None}, 'yfinance': {'multiple_items_allowed': True, 'choices': None}}, 'interval': {'fmp': {'choices': ['1m', '5m', '1h', '1d']}, 'tiingo': {'multiple_items_allowed': False, 'choices': ['1m', '5m', '15m', '30m', '90m', '1h', '2h', '4h', '1d', '7d', '30d']}, 'yfinance': {'multiple_items_allowed': False, 'choices': ['1m', '2m', '5m', '15m', '30m', '60m', '90m', '1h', '1d', '5d', '1W', '1M', '1Q']}}, 'exchanges': {'tiingo': {'multiple_items_allowed': True, 'choices': None}}},
            )
        )
