"""Autogenerated OpenBB /imf_utils Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
from typing import Annotated, Literal, Any
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_imf_utils(Container):
    """/imf_utils
    get_dataflow_dimensions
    list_dataflow_choices
    list_dataflows
    list_port_id_choices
    list_table_choices
    list_tables
    presentation_table
    presentation_table_choices
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def get_dataflow_dimensions(
        self,
        dataflow_id: Annotated[
            str,
            OpenBBField(
                description=(
                    'The IMF dataflow ID. Use `list_dataflows()` to see'
                    'available dataflows.'
                )
            )
        ],
        output_format: Annotated[
            Literal['json', 'markdown'],
            OpenBBField(
                description=''
            )
        ] = 'json',
        **kwargs: Any
    ) -> OBBject:
        """Dataflow parameters and possible values.

Returns an OBBject containing either a JSON dictionary of parameters
and their options, or a markdown string under the 'results' attribute.

Parameters
----------
dataflow_id : str
    The IMF dataflow ID. Use `list_dataflows()` to see available dataflows.
output_format : Optional[Literal['json', 'markdown']]

Returns
-------
OBBject

Examples
--------
>>> from openbb import obb
>>> # Get parameters for the 'CPI' dataflow.
>>> imf_params = obb.imf.utils.get_dataflow_dimensions('CPI')
>>> print(imf_params.results)
>>> # Get parameters for the 'GFS_BS' dataflow in markdown format.
>>> obb.imf_utils.get_dataflow_dimensions(dataflow_id='GFS_BS', output_format='markdown')
>>> # Get parameters for the 'IL' dataflow in JSON format.
>>> obb.imf_utils.get_dataflow_dimensions(dataflow_id='IL', output_format='json')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/get_dataflow_dimensions",
            **filter_inputs(
                dataflow_id=dataflow_id,
                output_format=output_format,
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def list_dataflow_choices(
        self,
        **kwargs: Any
    ) -> list:
        """Get dataflow choices for IMF data retrieval.

Returns
-------
list[dict[str, str]]
    A list of dictionaries with 'label' and 'value' for each presentation table.

Examples
--------
>>> from openbb import obb
>>> # Get dataflow choices for IMF data retrieval.
>>> obb.imf_utils.list_dataflow_choices()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/list_dataflow_choices",
            **filter_inputs(
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def list_dataflows(
        self,
        output_format: Annotated[
            Literal['json', 'markdown'],
            OpenBBField(
                description=''
            )
        ] = 'json',
        **kwargs: Any
    ) -> OBBject:
        """list all available IMF dataflows.

Returns an OBBject containing either a JSON dictionary of dataflows
or a markdown string under the 'results' attribute.

Parameters
----------
output_format : Optional[Literal['json', 'markdown']]

Returns
-------
OBBject

Examples
--------
>>> from openbb import obb
>>> # lists all known dataflows available from the IMF in JSON format.
>>> obb.imf_utils.list_dataflows(output_format='json')
>>> # Return the content as a markdown-formatted summary instead of a JSON table.
>>> obb.imf_utils.list_dataflows(output_format='markdown')
>>> # lists all known dataflows available from the IMF.
>>> imf_dataflows = obb.imf.utils.list_dataflows()
>>> print(imf_dataflows)

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/list_dataflows",
            **filter_inputs(
                output_format=output_format,
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def list_port_id_choices(
        self,
        **kwargs: Any
    ) -> list:
        """Get port ID choices for IMF Port Watch.

Returns
-------
list[dict[str, str]]
    A list of dictionaries with 'label' and 'value' for each port ID.

Examples
--------
>>> from openbb import obb
>>> # Get port ID choices for IMF Port Watch.
>>> obb.imf_utils.list_port_id_choices()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/list_port_id_choices",
            **filter_inputs(
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def list_table_choices(
        self,
        **kwargs: Any
    ) -> list:
        """Get presentation table choices for IMF data retrieval.

Returns
-------
list[dict[str, str]]
    A list of dictionaries with 'label' and 'value' for each presentation table.

Examples
--------
>>> from openbb import obb
>>> # Get presentation table choices for IMF data retrieval.
>>> obb.imf_utils.list_table_choices()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/list_table_choices",
            **filter_inputs(
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def list_tables(
        self,
        **kwargs: Any
    ) -> OBBject:
        """Get the list of presentation tables available from the IMF.

Returns
-------
OBBject[list[ImfTableMetadata]]

Examples
--------
>>> from openbb import obb
>>> # Get the list of available presentation tables.
>>> obb.imf_utils.list_tables()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/list_tables",
            **filter_inputs(
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def presentation_table(
        self,
        dataflow_group: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'The IMF dataflow group. See'
                    'presentation_table_choices() for options.'
                )
            )
        ] = None,
        table: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'The IMF presentation table ID. See'
                    'presentation_table_choices() for options.'
                )
            )
        ] = None,
        country: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'Country code to filter the data. Enter multiple'
                    "codes by joining on '+'. See"
                    'presentation_table_choices() for options. Typical'
                    'values are ISO3 country codes.'
                )
            )
        ] = None,
        frequency: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'The data frequency. See'
                    'presentation_table_choices() for options. Typical'
                    "values are 'A' (annual), 'Q' (quarter), 'M'"
                    "(month), or 'D' (day)."
                )
            )
        ] = None,
        dimension_values: Annotated[
            list[str] | str | None,
            OpenBBField(
                description=(
                    'Dimension selection for filtering. Format:'
                    "'DIM_ID1:VAL1+VAL2.' See"
                    'presentation_table_choices() and'
                    'list_dataflow_choices() for available dimensions'
                    'and values.'
                )
            )
        ] = None,
        limit: Annotated[
            int,
            OpenBBField(
                description='Maximum number of records to retrieve per series.'
            )
        ] = 1,
        raw: Annotated[
            bool,
            OpenBBField(
                description=(
                    'Return presentation table as raw JSON data if'
                    'True.'
                )
            )
        ] = False,
        **kwargs: Any
    ) -> Any:
        """Get a formatted presentation table from the IMF database. Returns as HTML or JSON list.

Parameters
----------
dataflow_group : str | None
    The IMF dataflow group. See presentation_table_choices() for options.
table : str | None
    The IMF presentation table ID. See presentation_table_choices() for options.
country : str | None
    Country code to filter the data. Enter multiple codes by joining on '+'. See presentation_table_choices() for options. Typical values are ISO3 country codes.
frequency : str | None
    The data frequency. See presentation_table_choices() for options. Typical values are 'A' (annual), 'Q' (quarter), 'M' (month), or 'D' (day).
dimension_values : list[str] | str | None
    Dimension selection for filtering. Format: 'DIM_ID1:VAL1+VAL2.' See presentation_table_choices() and list_dataflow_choices() for available dimensions and values.
limit : Optional[int]
    Maximum number of records to retrieve per series.
raw : Optional[bool]
    Return presentation table as raw JSON data if True.

Returns
-------
Any

Examples
--------
>>> from openbb import obb
>>> # Get the most recent Balance of Payments table for Japan.
>>> obb.imf_utils.presentation_table(dataflow_group='bop', table='bop_standard', country='JPN', frequency='Q', limit=4)

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/presentation_table",
            **filter_inputs(
                dataflow_group=dataflow_group,
                table=table,
                country=country,
                frequency=frequency,
                dimension_values=dimension_values,
                limit=limit,
                raw=raw,
                **kwargs,
            )
        )

    @exception_handler
    @validate
    def presentation_table_choices(
        self,
        dataflow_group: Annotated[
            str | None,
            OpenBBField(
                description='The IMF dataflow group. Show all groups if None.'
            )
        ] = None,
        table: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'The IMF presentation table ID. Enter a'
                    'dataflow_group to see table choices.'
                )
            )
        ] = None,
        country: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'Enter a dataflow_group and table to see country'
                    'choices.'
                )
            )
        ] = None,
        frequency: Annotated[
            str | None,
            OpenBBField(
                description=(
                    'Enter a dataflow_group, table, and country to see'
                    'frequency choices.'
                )
            )
        ] = None,
        **kwargs: Any
    ) -> list:
        """Get presentation table choices for IMF data retrieval.

This endpoint provides dynamic choices for IMF presentation tables based on selected parameters.
It is intended to be used by the OpenBB Workspace UI to populate dropdowns.

For manual API calls, use `economy/indicators` instead with a `symbol` from `list_tables()`.

Parameters
----------
dataflow_group : str | None
    The IMF dataflow group. Show all groups if None.
table : str | None
    The IMF presentation table ID. Enter a dataflow_group to see table choices.
country : str | None
    Enter a dataflow_group and table to see country choices.
frequency : str | None
    Enter a dataflow_group, table, and country to see frequency choices.

Returns
-------
list[dict[str, str]]
    A list of dictionaries with 'label' and 'value' for each presentation table.

Examples
--------
>>> from openbb import obb
>>> # Get the dataflow group choices.
>>> obb.imf_utils.presentation_table_choices()
>>> # Get table choices for the 'cpi' dataflow group.
>>> obb.imf_utils.presentation_table_choices(dataflow_group='cpi')
>>> # Get country choices for the 'cpi' dataflow group and 'cpi' table.
>>> obb.imf_utils.presentation_table_choices(dataflow_group='cpi', table='cpi')
>>> # Get frequency choices for the 'cpi' dataflow group, 'cpi' table, and 'JPN' country.
>>> obb.imf_utils.presentation_table_choices(dataflow_group='cpi', table='cpi', country='JPN')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/imf_utils/presentation_table_choices",
            **filter_inputs(
                dataflow_group=dataflow_group,
                table=table,
                country=country,
                frequency=frequency,
                **kwargs,
            )
        )
