"""Autogenerated OpenBB /fixedincome/rate Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_fixedincome_rate(Container):
    """/fixedincome/rate
    ameribor
    dpcredit
    ecb
    effr
    effr_forecast
    estr
    iorb
    overnight_bank_funding
    sofr
    sonia
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def ameribor(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """AMERIBOR.

AMERIBOR (short for the American interbank offered rate) is a benchmark interest rate that reflects the true cost of
short-term interbank borrowing. This rate is based on transactions in overnight unsecured loans conducted on the
American Financial Exchange (AFX).


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
maturity : str
    Period of AMERIBOR rate. Multiple comma separated items allowed. (provider: fred)
frequency : str
    Frequency aggregation to convert daily data to lower frequency.
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[Ameribor]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

Ameribor
--------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
maturity : str
    Maturity length of the item.
rate : float
    Interest rate.
title : str | None
    Title of the series.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.ameribor()
>>> # The change from one year ago is applied with the transform parameter.
>>> obb.fixedincome.rate.ameribor(maturity='all', transform='pc1')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/ameribor",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.ameribor",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'maturity': {'fred': {'multiple_items_allowed': True, 'choices': None}}, 'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def dpcredit(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Discount Window Primary Credit Rate.

A bank rate is the interest rate a nation's central bank charges to its domestic banks to borrow money.
The rates central banks charge are set to stabilize the economy.
In the United States, the Federal Reserve System's Board of Governors set the bank rate,
also known as the discount rate.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
parameter : str
    FRED series ID of DWPCR data. (provider: fred)
    Choices for fred: 'daily_excl_weekend', 'monthly', 'weekly', 'daily', 'annual'

Returns
-------
OBBject
    results : list[DiscountWindowPrimaryCreditRate]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

DiscountWindowPrimaryCreditRate
-------------------------------
date : date
    The date of the data.
rate : float | None
    Discount Window Primary Credit Rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.dpcredit()
>>> obb.fixedincome.rate.dpcredit(start_date='2023-02-01', end_date='2023-05-01')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/dpcredit",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.dpcredit",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'parameter': {'fred': {'choices': ['daily_excl_weekend', 'monthly', 'weekly', 'daily', 'annual']}}},
            )
        )

    @exception_handler
    @validate
    def ecb(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        interest_rate_type: Annotated[
            Literal['deposit', 'lending', 'refinancing'],
            OpenBBField(
                description='The type of interest rate.'
            )
        ] = 'lending',
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """European Central Bank Interest Rates.

The Governing Council of the ECB sets the key interest rates for the euro area:

- The interest rate on the main refinancing operations (MRO), which provide
the bulk of liquidity to the banking system.
- The rate on the deposit facility, which banks may use to make overnight deposits with the Eurosystem.
- The rate on the marginal lending facility, which offers overnight credit to banks from the Eurosystem.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
interest_rate_type : str
    The type of interest rate.

Returns
-------
OBBject
    results : list[EuropeanCentralBankInterestRates]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

EuropeanCentralBankInterestRates
--------------------------------
date : date
    The date of the data.
rate : float | None
    European Central Bank Interest Rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.ecb()
>>> obb.fixedincome.rate.ecb(interest_rate_type='refinancing')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/ecb",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.ecb",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                    "interest_rate_type": interest_rate_type,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def effr(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['federal_reserve', 'fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: federal_reserve, fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Fed Funds Rate.

Get Effective Federal Funds Rate data. A bank rate is the interest rate a nation's central bank charges to its
domestic banks to borrow money. The rates central banks charge are set to stabilize the economy.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: federal_reserve, fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'
effr_only : bool
    Return data without quantiles, target ranges, and volume. (provider: fred)

Returns
-------
OBBject
    results : list[FederalFundsRate]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

FederalFundsRate
----------------
date : date
    The date of the data.
rate : float
    Effective federal funds rate.
target_range_upper : float | None
    Upper bound of the target range.
target_range_lower : float | None
    Lower bound of the target range.
percentile_1 : float | None
    1st percentile of the distribution.
percentile_25 : float | None
    25th percentile of the distribution.
percentile_75 : float | None
    75th percentile of the distribution.
percentile_99 : float | None
    99th percentile of the distribution.
volume : float | None
    The trading volume.The notional volume of transactions (Billions of $).
intraday_low : float | None
    Intraday low. This field is only present for data before 2016. (provider: federal_reserve)
intraday_high : float | None
    Intraday high. This field is only present for data before 2016. (provider: federal_reserve)
standard_deviation : float | None
    Standard deviation. This field is only present for data before 2016. (provider: federal_reserve)
revision_indicator : str | None
    Indicates a revision of the data for that date. (provider: federal_reserve)

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.effr()
>>> obb.fixedincome.rate.effr(effr_only=True)

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/effr",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.effr",
                        ('federal_reserve', 'fred'),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def effr_forecast(
        self,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Fed Funds Rate Projections.

The projections for the federal funds rate are the value of the midpoint of the
projected appropriate target range for the federal funds rate or the projected
appropriate target level for the federal funds rate at the end of the specified
calendar year or over the longer run.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
long_run : bool
    Flag to show long run projections (provider: fred)

Returns
-------
OBBject
    results : list[PROJECTIONS]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

PROJECTIONS
-----------
date : date
    The date of the data.
range_high : float | None
    High projection of rates.
central_tendency_high : float | None
    Central tendency of high projection of rates.
median : float | None
    Median projection of rates.
range_midpoint : float | None
    Midpoint projection of rates.
central_tendency_midpoint : float | None
    Central tendency of midpoint projection of rates.
range_low : float | None
    Low projection of rates.
central_tendency_low : float | None
    Central tendency of low projection of rates.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.effr_forecast()
>>> obb.fixedincome.rate.effr_forecast(long_run=True)

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/effr_forecast",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.effr_forecast",
                        ('fred',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def estr(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Euro Short-Term Rate.

The euro short-term rate (€STR) reflects the wholesale euro unsecured overnight borrowing costs of banks located in
the euro area. The €STR is published on each TARGET2 business day based on transactions conducted and settled on
the previous TARGET2 business day (the reporting date “T”) with a maturity date of T+1 which are deemed to have been
executed at arm's length and thus reflect market rates in an unbiased way.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    Frequency aggregation to convert daily data to lower frequency.

    a = Annual

    q = Quarterly

    m = Monthly

    w = Weekly

    d = Daily

    wef = Weekly, Ending Friday

    weth = Weekly, Ending Thursday

    wew = Weekly, Ending Wednesday

    wetu = Weekly, Ending Tuesday

    wem = Weekly, Ending Monday

    wesu = Weekly, Ending Sunday

    wesa = Weekly, Ending Saturday

    bwew = Biweekly, Ending Wednesday

    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    A key that indicates the aggregation method used for frequency aggregation.

    avg = Average

    sum = Sum

    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type

    None = No transformation

    chg = Change

    ch1 = Change from Year Ago

    pch = Percent Change

    pc1 = Percent Change from Year Ago

    pca = Compounded Annual Rate of Change

    cch = Continuously Compounded Rate of Change

    cca = Continuously Compounded Annual Rate of Change

    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[EuroShortTermRate]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

EuroShortTermRate
-----------------
date : date
    The date of the data.
rate : float
    Volume-weighted trimmed mean rate.
percentile_25 : float | None
    Rate at 25th percentile of volume.
percentile_75 : float | None
    Rate at 75th percentile of volume.
volume : float | None
    The trading volume. (Millions of €EUR).
transactions : int | None
    Number of transactions.
number_of_banks : int | None
    Number of active banks.
large_bank_share_of_volume : float | None
    The percent of volume attributable to the 5 largest active banks.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.estr()
>>> obb.fixedincome.rate.estr(transform='ch1')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/estr",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.estr",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def iorb(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Interest on Reserve Balances.

Get Interest Rate on Reserve Balances data A bank rate is the interest rate a nation's central bank charges to its
domestic banks to borrow money. The rates central banks charge are set to stabilize the economy. In the
United States, the Federal Reserve System's Board of Governors set the bank rate, also known as the discount rate.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.

Returns
-------
OBBject
    results : list[IORB]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

IORB
----
date : date
    The date of the data.
rate : float | None
    IORB rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.iorb()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/iorb",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.iorb",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def overnight_bank_funding(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['federal_reserve', 'fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: federal_reserve, fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Overnight Bank Funding.

For the United States, the overnight bank funding rate (OBFR) is calculated as a volume-weighted median of
overnight federal funds transactions and Eurodollar transactions reported in the
FR 2420 Report of Selected Money Market Rates.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: federal_reserve, fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[OvernightBankFundingRate]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

OvernightBankFundingRate
------------------------
date : date
    The date of the data.
rate : float
    Overnight Bank Funding Rate.
percentile_1 : float | None
    1st percentile of the distribution.
percentile_25 : float | None
    25th percentile of the distribution.
percentile_75 : float | None
    75th percentile of the distribution.
percentile_99 : float | None
    99th percentile of the distribution.
volume : float | None
    The trading volume.The notional volume of transactions (Billions of $).
revision_indicator : str | None
    Indicates a revision of the data for that date. (provider: federal_reserve)

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.overnight_bank_funding()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/overnight_bank_funding",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.overnight_bank_funding",
                        ('federal_reserve', 'fred'),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def sofr(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['federal_reserve', 'fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: federal_reserve, fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Secured Overnight Financing Rate.

The Secured Overnight Financing Rate (SOFR) is a broad measure of the cost of
borrowing cash overnight collateralizing by Treasury securities.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: federal_reserve, fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[SOFR]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SOFR
----
date : date
    The date of the data.
rate : float
    Effective federal funds rate.
percentile_1 : float | None
    1st percentile of the distribution.
percentile_25 : float | None
    25th percentile of the distribution.
percentile_75 : float | None
    75th percentile of the distribution.
percentile_99 : float | None
    99th percentile of the distribution.
volume : float | None
    The trading volume.The notional volume of transactions (Billions of $).
average_30d : float | None
    30-Day Average SOFR (provider: fred)
average_90d : float | None
    90-Day Average SOFR (provider: fred)
average_180d : float | None
    180-Day Average SOFR (provider: fred)
index : float | None
    SOFR index as 2018-04-02 = 1 (provider: fred)

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.sofr()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/sofr",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.sofr",
                        ('federal_reserve', 'fred'),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def sonia(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Sterling Overnight Index Average.

SONIA (Sterling Overnight Index Average) is an important interest rate benchmark. SONIA is based on actual
transactions and reflects the average of the interest rates that banks pay to borrow sterling overnight from other
financial institutions and other institutional investors.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
parameter : str
    Period of SONIA rate. (provider: fred)
    Choices for fred: 'rate', 'index', '10th_percentile', '25th_percentile', '75th_percentile' '90th_percentile', 'total_nominal_value'

Returns
-------
OBBject
    results : list[SONIA]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SONIA
-----
date : date
    The date of the data.
rate : float | None
    SONIA rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.rate.sonia()
>>> obb.fixedincome.rate.sonia(parameter='total_nominal_value')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/rate/sonia",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.rate.sonia",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'parameter': {'fred': {'choices': ['rate', 'index', '10th_percentile', '25th_percentile', '75th_percentile', '90th_percentile', 'total_nominal_value']}}},
            )
        )
