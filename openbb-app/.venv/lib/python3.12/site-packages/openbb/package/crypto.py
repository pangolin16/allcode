"""Autogenerated OpenBB /crypto Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
from typing import Annotated, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField

class ROUTER_crypto(Container):
    """/crypto
    /price
    search
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @property
    def price(self):
        """Router for /crypto/price."""
        from . import crypto_price

        return crypto_price.ROUTER_crypto_price(command_runner=self._command_runner)

    @exception_handler
    @validate
    def search(
        self,
        query: Annotated[
            str | None,
            OpenBBField(
                description='Search query.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fmp']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fmp.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Search available cryptocurrency pairs within a provider.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fmp.
query : str | None
    Search query.

Returns
-------
OBBject
    results : list[CryptoSearch]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

CryptoSearch
------------
symbol : str
    Symbol representing the entity requested in the data. (Crypto)
name : str | None
    Name of the crypto.
exchange : str | None
    The exchange code the crypto trades on. (provider: fmp)
ico_date : date | None
    The ICO date of the token. (provider: fmp)
circulating_supply : float | None
    The circulating supply of the token. (provider: fmp)
total_supply : float | None
    The total supply of the token. (provider: fmp)

Examples
--------
>>> from openbb import obb
>>> obb.crypto.search()
>>> obb.crypto.search(query='BTCUSD')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/crypto/search",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "crypto.search",
                        ('fmp',),
                    )
                },
                standard_params={
                    "query": query,
                },
                extra_params=kwargs,
            )
        )
