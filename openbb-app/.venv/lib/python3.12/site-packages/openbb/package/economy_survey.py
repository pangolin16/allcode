"""Autogenerated OpenBB /economy/survey Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_economy_survey(Container):
    """/economy/survey
    bls_search
    bls_series
    economic_conditions_chicago
    inflation_expectations
    manufacturing_outlook_ny
    manufacturing_outlook_texas
    nonfarm_payrolls
    sloos
    university_of_michigan
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def bls_search(
        self,
        query: Annotated[
            str,
            OpenBBField(
                description=(
                    'The search word(s). Use semi-colon to separate'
                    'multiple queries as an & operator.'
                )
            )
        ] = '',
        provider: Annotated[
            Optional[Literal['bls']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: bls.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Search BLS surveys by category and keyword or phrase to identify BLS series IDs.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: bls.
query : str
    The search word(s). Use semi-colon to separate multiple queries as an & operator.
category : str
    The category of BLS survey to search within.
An empty search query will return all series within the category. Options are:

    cpi - Consumer Price Index

    pce - Personal Consumption Expenditure

    ppi - Producer Price Index

    ip - Industry Productivity

    jolts - Job Openings and Labor Turnover Survey

    nfp - Nonfarm Payrolls

    cps - Current Population Survey

    lfs - Labor Force Statistics

    wages - Wages

    ec - Employer Costs

    sla - State and Local Area Employment

    bed - Business Employment Dynamics

    tu - Time Use
 (provider: bls)
    Choices for bls: 'cpi', 'pce', 'ppi', 'ip', 'jolts', 'nfp', 'cps', 'lfs', 'wages', 'ec', 'sla' 'bed', 'tu'
include_extras : bool
    Include additional information in the search results. Extra fields returned are metadata and vary by survey. Fields are undefined strings that typically have names ending with '_code'. (provider: bls)
include_code_map : bool
    When True, includes the complete code map for eaçh survey in the category, returned separately as a nested JSON to the `extras['results_metadata']` property of the response. Example content is the NAICS industry map for PPI surveys. Each code is a value within the 'symbol' of the time series. (provider: bls)

Returns
-------
OBBject
    results : list[BlsSearch]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

BlsSearch
---------
symbol : str
    Symbol representing the entity requested in the data.
title : str | None
    The title of the series.
survey_name : str | None
    The name of the survey.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.bls_search(category='cpi')
>>> # Use semi-colon to separate multiple queries as an & operator.
>>> obb.economy.survey.bls_search(category='cpi', query='seattle;gasoline')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/bls_search",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.bls_search",
                        ('bls',),
                    )
                },
                standard_params={
                    "query": query,
                },
                extra_params=kwargs,
                info={'category': {'bls': {'multiple_items_allowed': False, 'choices': ['cpi', 'pce', 'ppi', 'ip', 'jolts', 'nfp', 'cps', 'lfs', 'wages', 'ec', 'sla', 'bed', 'tu']}}},
            )
        )

    @exception_handler
    @validate
    def bls_series(
        self,
        symbol: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    'Symbol to get data for. Multiple comma separated'
                    'items allowed for provider(s): bls.'
                )
            )
        ],
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['bls']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: bls.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get time series data for one, or more, BLS series IDs.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: bls.
symbol : str | list[str]
    Symbol to get data for. Multiple comma separated items allowed for provider(s): bls.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
calculations : bool
    Include calculations in the response, if available. Default is True. (provider: bls)
annual_average : bool
    Include annual averages in the response, if available. Default is False. (provider: bls)
aspects : bool
    Include all aspects associated with a data point for a given BLS series ID, if available. Returned with the series metadata, under `extras` of the response object. Default is False. (provider: bls)

Returns
-------
OBBject
    results : list[BlsSeries]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

BlsSeries
---------
date : date
    The date of the data.
symbol : str
    Symbol representing the entity requested in the data.
title : str | None
    Title of the series.
value : float | None
    Observation value for the symbol and date.
change_1_m : float | None
    One month change in value. (provider: bls)
change_3_m : float | None
    Three month change in value. (provider: bls)
change_6_m : float | None
    Six month change in value. (provider: bls)
change_12_m : float | None
    One year change in value. (provider: bls)
change_percent_1_m : float | None
    One month change in percent. (provider: bls)
change_percent_3_m : float | None
    Three month change in percent. (provider: bls)
change_percent_6_m : float | None
    Six month change in percent. (provider: bls)
change_percent_12_m : float | None
    One year change in percent. (provider: bls)
latest : bool | None
    Latest value indicator. (provider: bls)
footnotes : str | None
    Footnotes accompanying the value. (provider: bls)

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.bls_series(symbol='CES0000000001')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/bls_series",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.bls_series",
                        ('bls',),
                    )
                },
                standard_params={
                    "symbol": symbol,
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'symbol': {'bls': {'multiple_items_allowed': True, 'choices': None}}},
            )
        )

    @exception_handler
    @validate
    def economic_conditions_chicago(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get The Survey Of Economic Conditions For The Chicago Region.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    Frequency aggregation to convert monthly data to lower frequency. None is monthly. (provider: fred)
    Choices for fred: 'annual', 'quarter'
aggregation_method : str
    A key that indicates the aggregation method used for frequency aggregation.

    avg = Average

    sum = Sum

    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type

    None = No transformation

    chg = Change

    ch1 = Change from Year Ago

    pch = Percent Change

    pc1 = Percent Change from Year Ago

    pca = Compounded Annual Rate of Change

    cch = Continuously Compounded Rate of Change

    cca = Continuously Compounded Annual Rate of Change

    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[SurveyOfEconomicConditionsChicago]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SurveyOfEconomicConditionsChicago
---------------------------------
date : date
    The date of the data.
activity_index : float | None
    Activity Index.
one_year_outlook : float | None
    One Year Outlook Index.
manufacturing_activity : float | None
    Manufacturing Activity Index.
non_manufacturing_activity : float | None
    Non-Manufacturing Activity Index.
capital_expenditures_expectations : float | None
    Capital Expenditures Expectations Index.
hiring_expectations : float | None
    Hiring Expectations Index.
current_hiring : float | None
    Current Hiring Index.
labor_costs : float | None
    Labor Costs Index.
non_labor_costs : float | None
    Non-Labor Costs Index.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.economic_conditions_chicago()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/economic_conditions_chicago",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.economic_conditions_chicago",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['annual', 'quarter']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def inflation_expectations(
        self,
        provider: Annotated[
            Optional[Literal['federal_reserve']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: federal_reserve.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Survey of forward inflation expectations from the Survey of Professional Forecasters.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: federal_reserve.
start_date : date | None
    Start date of the data, in YYYY-MM-DD format. Data begins from 1970-04-01 and is quarterly. (provider: federal_reserve)
end_date : date | None
    End date of the data, in YYYY-MM-DD format. (provider: federal_reserve)

Returns
-------
OBBject
    results : list[InflationExpectations]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

InflationExpectations
---------------------
date : date | None
    The date of the survey (first day of the survey quarter). (provider: federal_reserve)
infpgdp1yr : float | None
    One-year-ahead annual-average GDP price index inflation forecast. Computed from median forecasts of GDP price index levels (PGDP2 and PGDP6). Formula: ((PGDP6/PGDP2) - 1) * 100. Available from 1970:Q2. (provider: federal_reserve)
infcpi1yr : float | None
    One-year-ahead annual-average CPI inflation forecast. Computed as the geometric average of quarter-over-quarter median CPI forecasts (CPI3 to CPI6). Available from 1981:Q3. (provider: federal_reserve)
infcpi10yr : float | None
    Ten-year-ahead annual-average CPI inflation forecast. Expected average annual rate of CPI inflation over the next 10 years. Available from 1991:Q4. (provider: federal_reserve)

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.inflation_expectations()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/inflation_expectations",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.inflation_expectations",
                        ('federal_reserve',),
                    )
                },
                standard_params={
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def manufacturing_outlook_ny(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get the Empire State Manufacturing Survey.

It is a monthly survey of manufacturers in New York State conducted by the Federal Reserve Bank of New York.

Participants from across the state in a variety of industries respond to a questionnaire
and report the change in a variety of indicators from the previous month.

Respondents also state the likely direction of these same indicators six months ahead.
April 2002 is the first report, although survey data date back to July 2001.

The survey is sent on the first day of each month to the same pool of about 200
manufacturing executives in New York State, typically the president or CEO.

About 100 responses are received. Most are completed by the tenth, although surveys are accepted until the fifteenth.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
topic : str
    The topic for the survey response. Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'business_outlook', 'hours_worked', 'employment', 'inventories' 'prices_received', 'prices_paid', 'capex', 'unfilled_orders', 'new_orders' 'shipments', 'delivery_times'
seasonally_adjusted : bool
    Whether the data is seasonally adjusted, default is False (provider: fred)
frequency : str
    Frequency aggregation to convert monthly data to lower frequency. None is monthly. (provider: fred)
    Choices for fred: 'quarter', 'annual'
aggregation_method : str
    A key that indicates the aggregation method used for frequency aggregation.
avg = Average
sum = Sum
eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type
None = No transformation
chg = Change
ch1 = Change from Year Ago
pch = Percent Change
pc1 = Percent Change from Year Ago
pca = Compounded Annual Rate of Change
cch = Continuously Compounded Rate of Change
cca = Continuously Compounded Annual Rate of Change
log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[ManufacturingOutlookNY]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

ManufacturingOutlookNY
----------------------
date : date
    The date of the data.
topic : str | None
    Topic of the survey response.
diffusion_index : float | None
    Diffusion Index.
percent_reporting_increase : float | None
    Percent of respondents reporting an increase over the last month.
percent_reporting_decrease : float | None
    Percent of respondents reporting a decrease over the last month.
percent_reporting_no_change : float | None
    Percent of respondents reporting no change over the last month.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.manufacturing_outlook_ny()
>>> obb.economy.survey.manufacturing_outlook_ny(topic='hours_worked,new_orders', transform='pc1', seasonally_adjusted=True)

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/manufacturing_outlook_ny",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.manufacturing_outlook_ny",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'topic': {'fred': {'multiple_items_allowed': True, 'choices': ['business_outlook', 'hours_worked', 'employment', 'inventories', 'prices_received', 'prices_paid', 'capex', 'unfilled_orders', 'new_orders', 'shipments', 'delivery_times'], 'x-widget_config': {'value': 'new_orders'}}}, 'frequency': {'fred': {'choices': ['quarter', 'annual']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def manufacturing_outlook_texas(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get The Manufacturing Outlook Survey For The Texas Region.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
topic : str
    The topic for the survey response. Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'business_activity', 'business_outlook', 'capex', 'prices_paid', 'production' 'inventory', 'new_orders', 'new_orders_growth', 'unfilled_orders', 'shipments' 'delivery_time', 'employment', 'wages', 'hours_worked'
frequency : str
    
Frequency aggregation to convert monthly data to lower frequency. None is monthly.
 (provider: fred)
    Choices for fred: 'annual', 'quarter'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[ManufacturingOutlookTexas]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

ManufacturingOutlookTexas
-------------------------
date : date
    The date of the data.
topic : str | None
    Topic of the survey response.
diffusion_index : float | None
    Diffusion Index.
percent_reporting_increase : float | None
    Percent of respondents reporting an increase over the last month.
percent_reporting_decrease : float | None
    Percent of respondents reporting a decrease over the last month.
percent_reporting_no_change : float | None
    Percent of respondents reporting no change over the last month.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.manufacturing_outlook_texas()
>>> obb.economy.survey.manufacturing_outlook_texas(topic='business_outlook,new_orders', transform='pc1')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/manufacturing_outlook_texas",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.manufacturing_outlook_texas",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'topic': {'fred': {'multiple_items_allowed': True, 'choices': ['business_activity', 'business_outlook', 'capex', 'prices_paid', 'production', 'inventory', 'new_orders', 'new_orders_growth', 'unfilled_orders', 'shipments', 'delivery_time', 'employment', 'wages', 'hours_worked']}}, 'frequency': {'fred': {'choices': ['annual', 'quarter']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def nonfarm_payrolls(
        self,
        date: Annotated[
            Union[datetime.date, str, None, list[datetime.date | str | None]],
            OpenBBField(
                description=(
                    'A specific date to get data for. Default is the'
                    'latest report. Multiple comma separated items'
                    'allowed for provider(s): fred.'
                )
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get Nonfarm Payrolls Survey.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
date : date | str | list[date | None] | None
    A specific date to get data for. Default is the latest report. Multiple comma separated items allowed for provider(s): fred.
category : str
    The category to query. (provider: fred)
    Choices for fred: 'employees_nsa', 'employees_sa', 'employees_production_and_nonsupervisory' 'employees_women', 'employees_women_percent', 'avg_hours' 'avg_hours_production_and_nonsupervisory', 'avg_hours_overtime' 'avg_hours_overtime_production_and_nonsupervisory', 'avg_earnings_hourly' 'avg_earnings_hourly_production_and_nonsupervisory', 'avg_earnings_weekly' 'avg_earnings_weekly_production_and_nonsupervisory', 'index_weekly_hours' 'index_weekly_hours_production_and_nonsupervisory', 'index_weekly_payrolls' 'index_weekly_payrolls_production_and_nonsupervisory'

Returns
-------
OBBject
    results : list[NonFarmPayrolls]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

NonFarmPayrolls
---------------
date : date
    The date of the data.
symbol : str
    Symbol representing the entity requested in the data.
value : float
    
name : str | None
    The name of the series. (provider: fred)
element_id : str | None
    The element id in the parent/child relationship. (provider: fred)
parent_id : str | None
    The parent id in the parent/child relationship. (provider: fred)
children : str | None
    The element_id of each child, as a comma-separated string. (provider: fred)
level : int | None
    The indentation level of the element. (provider: fred)

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.nonfarm_payrolls()
>>> obb.economy.survey.nonfarm_payrolls(category='avg_hours')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/nonfarm_payrolls",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.nonfarm_payrolls",
                        ('fred',),
                    )
                },
                standard_params={
                    "date": date,
                },
                extra_params=kwargs,
                info={'date': {'fred': {'multiple_items_allowed': True, 'choices': None}}, 'category': {'fred': {'choices': ['employees_nsa', 'employees_sa', 'employees_production_and_nonsupervisory', 'employees_women', 'employees_women_percent', 'avg_hours', 'avg_hours_production_and_nonsupervisory', 'avg_hours_overtime', 'avg_hours_overtime_production_and_nonsupervisory', 'avg_earnings_hourly', 'avg_earnings_hourly_production_and_nonsupervisory', 'avg_earnings_weekly', 'avg_earnings_weekly_production_and_nonsupervisory', 'index_weekly_hours', 'index_weekly_hours_production_and_nonsupervisory', 'index_weekly_payrolls', 'index_weekly_payrolls_production_and_nonsupervisory']}}},
            )
        )

    @exception_handler
    @validate
    def sloos(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get Senior Loan Officers Opinion Survey.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
category : str
    Category of survey response. (provider: fred)
    Choices for fred: 'spreads', 'consumer', 'auto', 'credit_card', 'firms', 'mortgage' 'commercial_real_estate', 'standards', 'demand', 'foreign_banks'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[SeniorLoanOfficerSurvey]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SeniorLoanOfficerSurvey
-----------------------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
value : float
    Survey value.
title : str | None
    Survey title.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.sloos()
>>> obb.economy.survey.sloos(category='credit_card')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/sloos",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.sloos",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'category': {'fred': {'choices': ['spreads', 'consumer', 'auto', 'credit_card', 'firms', 'mortgage', 'commercial_real_estate', 'standards', 'demand', 'foreign_banks']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def university_of_michigan(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get University of Michigan Consumer Sentiment and Inflation Expectations Surveys.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
frequency : str
    Frequency aggregation to convert monthly data to lower frequency. None is monthly. (provider: fred)
    Choices for fred: 'quarter', 'annual'
aggregation_method : str
    A key that indicates the aggregation method used for frequency aggregation.

    avg = Average

    sum = Sum

    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type

    None = No transformation

    chg = Change

    ch1 = Change from Year Ago

    pch = Percent Change

    pc1 = Percent Change from Year Ago

    pca = Compounded Annual Rate of Change

    cch = Continuously Compounded Rate of Change

    cca = Continuously Compounded Annual Rate of Change

    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[UniversityOfMichigan]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

UniversityOfMichigan
--------------------
date : date
    The date of the data.
consumer_sentiment : float | None
    Index of the results of the University of Michigan's monthly Survey of Consumers, which is used to estimate future spending and saving.  (1966:Q1=100).
inflation_expectation : float | None
    Median expected price change next 12 months, Surveys of Consumers.

Examples
--------
>>> from openbb import obb
>>> obb.economy.survey.university_of_michigan()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/economy/survey/university_of_michigan",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "economy.survey.university_of_michigan",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'frequency': {'fred': {'choices': ['quarter', 'annual']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )
