"""Autogenerated OpenBB /fixedincome/corporate Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_fixedincome_corporate(Container):
    """/fixedincome/corporate
    commercial_paper
    hqm
    spot_rates
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def commercial_paper(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Commercial Paper.

Commercial paper (CP) consists of short-term, promissory notes issued primarily by corporations.
Maturities range up to 270 days but average about 30 days.
Many companies use CP to raise cash needed for current transactions,
and many find it to be a lower-cost alternative to bank loans.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
maturity : str
    A target maturity. Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'all', 'overnight', '7d', '15d', '30d', '60d', '90d'
category : str
    The category of asset. Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'all', 'asset_backed', 'financial', 'nonfinancial', 'a2p2'
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew' 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[CommercialPaper]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

CommercialPaper
---------------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
maturity : str
    Maturity length of the item.
rate : float
    Interest rate.
title : str | None
    Title of the series.
asset_type : Optional[None]
    The category of asset. (provider: fred)

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.corporate.commercial_paper()
>>> obb.fixedincome.corporate.commercial_paper(category='all', maturity='15d')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/corporate/commercial_paper",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.corporate.commercial_paper",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'maturity': {'fred': {'multiple_items_allowed': True, 'choices': ['all', 'overnight', '7d', '15d', '30d', '60d', '90d']}}, 'category': {'fred': {'multiple_items_allowed': True, 'choices': ['all', 'asset_backed', 'financial', 'nonfinancial', 'a2p2']}}, 'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @exception_handler
    @validate
    def hqm(
        self,
        date: Annotated[
            Union[datetime.date, str, None, list[datetime.date | str | None]],
            OpenBBField(
                description=(
                    'A specific date to get data for. Multiple comma'
                    'separated items allowed for provider(s): fred.'
                )
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """High Quality Market Corporate Bond.

The HQM yield curve represents the high quality corporate bond market, i.e.,
corporate bonds rated AAA, AA, or A.  The HQM curve contains two regression terms.
These terms are adjustment factors that blend AAA, AA, and A bonds into a single HQM yield curve
that is the market-weighted average (MWA) quality of high quality bonds.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
date : date | str | list[date | None] | None
    A specific date to get data for. Multiple comma separated items allowed for provider(s): fred.
yield_curve : str
    The yield curve type. (provider: fred)
    Choices for fred: 'spot', 'par'

Returns
-------
OBBject
    results : list[HighQualityMarketCorporateBond]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

HighQualityMarketCorporateBond
------------------------------
date : date
    The date of the data.
rate : float
    Interest rate.
maturity : str
    Maturity.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.corporate.hqm()
>>> obb.fixedincome.corporate.hqm(yield_curve='par')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/corporate/hqm",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.corporate.hqm",
                        ('fred',),
                    )
                },
                standard_params={
                    "date": date,
                },
                extra_params=kwargs,
                info={'date': {'fred': {'multiple_items_allowed': True, 'choices': None}}, 'yield_curve': {'fred': {'choices': ['spot', 'par']}}},
            )
        )

    @exception_handler
    @validate
    def spot_rates(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        maturity: Annotated[
            Union[float, str, list[float | str]],
            OpenBBField(
                description=(
                    'Maturities in years. Multiple comma separated'
                    'items allowed for provider(s): fred.'
                )
            )
        ] = 10.0,
        category: Annotated[
            Union[str, list[str]],
            OpenBBField(
                description=(
                    'Rate category. Options: spot_rate, par_yield.'
                    'Multiple comma separated items allowed for'
                    "provider(s): fred. Choices for fred: 'par_yield',"
                    "'spot_rate'"
                )
            )
        ] = 'spot_rate',
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Spot Rates.

The spot rates for any maturity is the yield on a bond that provides a single payment at that maturity.
This is a zero coupon bond.
Because each spot rate pertains to a single cashflow, it is the relevant interest rate
concept for discounting a pension liability at the same maturity.


Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
maturity : float | str | list[float | str]
    Maturities in years. Multiple comma separated items allowed for provider(s): fred.
category : str | list[str]
    Rate category. Options: spot_rate, par_yield. Multiple comma separated items allowed for provider(s): fred.
    Choices for fred: 'par_yield', 'spot_rate'

Returns
-------
OBBject
    results : list[SpotRate]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

SpotRate
--------
date : date
    The date of the data.
rate : float | None
    Spot Rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.corporate.spot_rates()
>>> obb.fixedincome.corporate.spot_rates(maturity='10,20,30,50')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/corporate/spot_rates",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.corporate.spot_rates",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                    "maturity": maturity,
                    "category": category,
                },
                extra_params=kwargs,
                info={'maturity': {'fred': {'multiple_items_allowed': True, 'choices': None}}, 'category': {'fred': {'multiple_items_allowed': True, 'choices': ['par_yield', 'spot_rate']}}},
            )
        )
