"""Autogenerated OpenBB /fixedincome Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_fixedincome(Container):
    """/fixedincome
    bond_indices
    /corporate
    /government
    mortgage_indices
    /rate
    /spreads
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def bond_indices(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        index_type: Annotated[
            Literal['yield', 'yield_to_worst', 'total_return', 'oas'],
            OpenBBField(
                description=(
                    'The type of series. OAS is the option-adjusted'
                    'spread. Default is yield.'
                )
            )
        ] = 'yield',
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Bond Indices.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
index_type : str
    The type of series. OAS is the option-adjusted spread. Default is yield.
category : str
    The type of index category. Used in conjunction with 'index', default is 'us'. (provider: fred)
    Choices for fred: 'high_yield', 'us', 'emerging_markets'
index : str
    The specific index to query. Used in conjunction with 'category' and 'index_type', default is 'yield_curve'.
Possible values are:
    corporate
    seasoned_corporate
    liquid_corporate
    yield_curve
    crossover
    public_sector
    private_sector
    non_financial
    high_grade
    high_yield
    liquid_emea
    emea
    liquid_asia
    asia
    liquid_latam
    latam
    liquid_aaa
    liquid_bbb
    aaa
    aa
    a
    bbb
    bb
    b
    ccc

 Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'a', 'aa', 'aaa', 'asia', 'b', 'bb', 'bbb', 'ccc', 'corporate', 'crossover' 'emea', 'high_grade', 'high_yield', 'latam', 'liquid_aaa', 'liquid_asia' 'liquid_bbb', 'liquid_corporate', 'liquid_emea', 'liquid_latam', 'non_financial' 'private_sector', 'public_sector', 'seasoned_corporate', 'yield_curve'
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    None = No change
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    d = Daily
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'd', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa' 'bwew', 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
This parameter has no affect if the frequency parameter is not set, default is 'avg'.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[BondIndices]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

BondIndices
-----------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
value : float
    Index values.
maturity : str | None
    The maturity range of the bond index. Only applicable when 'index' is 'yield_curve'. (provider: fred)
title : str | None
    The title of the index. (provider: fred)

Examples
--------
>>> from openbb import obb
>>> # The default state for FRED are series for constructing the US Corporate Bond Yield Curve.
>>> obb.fixedincome.bond_indices()
>>> # Multiple indices, from within the same 'category', can be requested.
>>> obb.fixedincome.bond_indices(category='high_yield', index='us,europe,emerging', index_type='total_return')
>>> # From FRED, there are three main categories, 'high_yield', 'us', and 'emerging_markets'. Emerging markets is a broad category.
>>> obb.fixedincome.bond_indices(category='emerging_markets', index='corporate,private_sector,public_sector')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/bond_indices",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.bond_indices",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                    "index_type": index_type,
                },
                extra_params=kwargs,
                info={'category': {'fred': {'choices': ['high_yield', 'us', 'emerging_markets']}}, 'index': {'fred': {'multiple_items_allowed': True, 'choices': ['a', 'aa', 'aaa', 'asia', 'b', 'bb', 'bbb', 'ccc', 'corporate', 'crossover', 'emea', 'high_grade', 'high_yield', 'latam', 'liquid_aaa', 'liquid_asia', 'liquid_bbb', 'liquid_corporate', 'liquid_emea', 'liquid_latam', 'non_financial', 'private_sector', 'public_sector', 'seasoned_corporate', 'yield_curve']}}, 'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'd', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @property
    def corporate(self):
        """Router for /fixedincome/corporate."""
        from . import fixedincome_corporate

        return fixedincome_corporate.ROUTER_fixedincome_corporate(command_runner=self._command_runner)

    @property
    def government(self):
        """Router for /fixedincome/government."""
        from . import fixedincome_government

        return fixedincome_government.ROUTER_fixedincome_government(command_runner=self._command_runner)

    @exception_handler
    @validate
    def mortgage_indices(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Mortgage Indices.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
index : str
    The specific index, or index group, to query. Default is the 'primary' group. Multiple comma separated items allowed. (provider: fred)
    Choices for fred: 'primary', 'ltv_lte_80', 'ltv_gt_80', 'conforming_30y', 'conforming_30y_na' 'jumbo_30y', 'fha_30y', 'va_30y', 'usda_30y', 'conforming_15y' 'ltv_lte80_fico_ge740', 'ltv_lte80_fico_a720b739', 'ltv_lte80_fico_a700b719' 'ltv_lte80_fico_a680b699', 'ltv_lte80_fico_lt680', 'ltv_gt80_fico_ge740' 'ltv_gt80_fico_a720b739', 'ltv_gt80_fico_a700b719', 'ltv_gt80_fico_a680b699' 'ltv_gt80_fico_lt680'
frequency : str
    
Frequency aggregation to convert daily data to lower frequency.
    None = No change
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    d = Daily
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'd', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa' 'bwew', 'bwem'
aggregation_method : str
    
A key that indicates the aggregation method used for frequency aggregation.
This parameter has no affect if the frequency parameter is not set, default is 'avg'.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    
Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
    log = Natural Log
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log'

Returns
-------
OBBject
    results : list[MortgageIndices]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

MortgageIndices
---------------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
name : str | None
    Name of the index.
rate : float
    Mortgage rate.

Examples
--------
>>> from openbb import obb
>>> # The default state for FRED are the primary mortgage indices from Optimal Blue.
>>> obb.fixedincome.mortgage_indices()
>>> # Multiple indices can be requested.
>>> obb.fixedincome.mortgage_indices(index='jumbo_30y,conforming_30y,conforming_15y')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/mortgage_indices",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.mortgage_indices",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
                info={'index': {'fred': {'multiple_items_allowed': True, 'choices': ['primary', 'ltv_lte_80', 'ltv_gt_80', 'conforming_30y', 'conforming_30y_na', 'jumbo_30y', 'fha_30y', 'va_30y', 'usda_30y', 'conforming_15y', 'ltv_lte80_fico_ge740', 'ltv_lte80_fico_a720b739', 'ltv_lte80_fico_a700b719', 'ltv_lte80_fico_a680b699', 'ltv_lte80_fico_lt680', 'ltv_gt80_fico_ge740', 'ltv_gt80_fico_a720b739', 'ltv_gt80_fico_a700b719', 'ltv_gt80_fico_a680b699', 'ltv_gt80_fico_lt680']}}, 'frequency': {'fred': {'choices': ['a', 'q', 'm', 'w', 'd', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa', 'bwew', 'bwem']}}, 'aggregation_method': {'fred': {'choices': ['avg', 'sum', 'eop']}}, 'transform': {'fred': {'choices': ['chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca', 'log']}}},
            )
        )

    @property
    def rate(self):
        """Router for /fixedincome/rate."""
        from . import fixedincome_rate

        return fixedincome_rate.ROUTER_fixedincome_rate(command_runner=self._command_runner)

    @property
    def spreads(self):
        """Router for /fixedincome/spreads."""
        from . import fixedincome_spreads

        return fixedincome_spreads.ROUTER_fixedincome_spreads(command_runner=self._command_runner)
